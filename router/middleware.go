package router

// Implement Auth logic. Add .env string auth and most of all add an external auth that will be supported only by sender and processed here(?)
// idk since auth is not directly needed, but we need a more flexible control over situation happening in that system
